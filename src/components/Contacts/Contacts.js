import style from './Contacts.module.css';

const Contacts = ({
    match,
    history
}) => {
    // if (Math.random() > 0.6) {
    //     return <Redirect to="/" />
        //  history.push('/');
         
        //  return null;
    // }
    return(
        <h1>Contacts Page</h1>
    );
}

export default Contacts;